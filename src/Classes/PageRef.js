/** Page references, tracked in state for UI purposes. */
export default class PageRef{
    /**
     * @param {string} title 
     * @param {array} path 
     */
    constructor (title, path){
        this.title = title;
        this.path = path;
    }
}
